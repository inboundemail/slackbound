{
  "name": "slackbound",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "clean": "turbo run clean",
    "app:db:generate": " cd apps/dashboard && bunx drizzle-kit generate",
    "app:db:push": "cd apps/dashboard && bunx drizzle-kit push"
    
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "bun@1.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "better-auth": "^1.3.34",
    "geist": "^1.5.1",
    "lucide-react": "^0.548.0"
  }
}
